import { Theme } from "@aws-amplify/ui-react";

const theme: Theme = {
  name: "cra-hacklytics-theme",
  tokens: {
    colors: {
      font: {
        primary: { value: "#000" },
        
      },
    },
  },
};

export default theme;